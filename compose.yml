services:
  t-schedule:
    image: ghcr.io/lentryd/t-schedule-go:latest
    container_name: t-schedule
    hostname: t-schedule
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./credentials.json:/credentials.json:ro
